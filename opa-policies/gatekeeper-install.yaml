# Install OPA Gatekeeper on EKS
# Run: kubectl apply -f https://raw.githubusercontent.com/open-policy-agent/gatekeeper/release-3.14/deploy/gatekeeper.yaml
# Or use this simplified version for demo:
apiVersion: v1
kind: Namespace
metadata:
  name: gatekeeper-system
---
# Note: For demo, you can just run:
# kubectl apply -f https://raw.githubusercontent.com/open-policy-agent/gatekeeper/release-3.14/deploy/gatekeeper.yaml